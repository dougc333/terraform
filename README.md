terraform configs
1pod: 1 kube pod running helloworld server
add prometheus, grafana and load script. verify graphs make sense
run locally under kube

1pod_gcp: runs under gcp. 

2pod: runs locally showing scaling
2pod_gcp



