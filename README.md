This is week1. Simple demo of getting chatGPT to generate terraform and k8 code first shot. Quite impressive. 

This is a long way from production usefullness. We are only running a LLM server after a FifoQueue. 

We are missing: 
Pods for data cleaning
Pods for SFT
Pods for FT
Pods for Policy gradient
Pods for datastorage and versioning
Pipeline coordination. How to run pods after each other
Exception management. how ot cancel a pod if it is not doing useful work
ie. if not converging. There has to be a UI better than a training loss curve. For RL jobs you can tell by increasing time of stable walking the algorithm is getting better, Make this signal availalbe to the UI. 



terraform configs generated by ChatGPT. 
1pod: 1 kube pod running helloworld server
1pod_llm; 1 pod running llama.cpp, autoscaling? 
1podllm_queue: fifo queue, llm pods running llama.cpp with Gwen-.5B, input mpbb. 9/13 success rate. 
with prometheus, grafana and load script. 

1pod_gcp: runs under gcp. Waiting for quota. 

2pod: runs locally showing scaling 1->2->1
2pod_gcp: waiting quota
2pods_grafana_prom: 2 pods scaling with metrics. 



